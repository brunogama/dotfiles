#!/usr/bin/env bash
# clear_xcode
# author: Bruno Gama <bgamap at gmail dot com>

# pkill -9 -fi xcode
rm -rf ${HOME}/Library/Developer/Xcode/DerivedData/*
find /var/folders -iname "com.apple.DeveloperTools" -exec rm -rf {}/* \;

cat <<-__HEREDOC__

Finished wiping Xcode Derived Data.

__HEREDOC__
