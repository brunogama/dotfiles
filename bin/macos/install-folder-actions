#!/usr/bin/env bash
# Install the repository's macOS Folder Actions scripts through the shared linker.

set -euo pipefail

DOTFILES_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
readonly DOTFILES_ROOT

exec python3 "$DOTFILES_ROOT/bin/core/link-dotfiles.py" --folder-actions "$@"
