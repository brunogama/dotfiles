#!/usr/bin/env bash

set -euo pipefail

echo "nix-npm-sync is deprecated; use mise-toolchain-sync for supported security updates" >&2
script_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$script_dir/mise-toolchain-sync" "$@"
