#!/usr/bin/env bash

# CONFIGURATION=RELEASE
# Z_LINTER_SUPPORT_ENABLED=1
# Z_REMOVE_TESTS=1
open -a Xcode "$@" \
  --args \
  -CONFIGURATION=RELEASE \
  -Z_LINTER_SUPPORT_ENABLED=1 \
  -Z_REMOVE_TESTS=1
