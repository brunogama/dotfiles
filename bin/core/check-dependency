#!/usr/bin/env bash

set -euo pipefail

[[ $# -eq 1 ]] || { echo "Usage: check-dependency COMMAND" >&2; exit 2; }
if ! command -v "$1" >/dev/null 2>&1; then
    echo "$1 is missing. Run ./install to activate the managed Nix toolchain." >&2
    exit 1
fi
