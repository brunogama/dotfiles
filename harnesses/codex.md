# Codex harness

Codex uses `AGENTS.md` and shared skills under `.agents/skills/`. Agent role prompts live under `.agents/agents/` as project reference material.

`.codex/prompts/` is generated as a project convention. If the installed client does not discover project prompts, paste or invoke these prompts manually.
