# Plans

This directory contains the dependency-aware implementation plans for the convention-based linking architecture. Each plan's YAML frontmatter is the authoritative status and dependency graph; do not maintain a second static DAG here.

The plans implement `specs/linking-architecture.md` in dependency order.
